import { h } from '../ui.js';

/** Placeholder until Phase 5 wires the real expenses API and view. */
export async function renderExpenses() {
  return h('div', { class: 'empty' }, 'Expenses is coming soon.');
}
