import { h } from '../ui.js';

/** Placeholder until Phase 5 wires the real lockers API and view. */
export async function renderLockers() {
  return h('div', { class: 'empty' }, 'Lockers is coming soon.');
}
